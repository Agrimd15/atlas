{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c 'import yfinance, requests' 2>/dev/null || pip3 install -q -r \"${CLAUDE_PLUGIN_ROOT}/requirements.txt\" >/dev/null 2>&1 || true"
          }
        ]
      }
    ]
  }
}
